meta {
  name: Have address parameter
  type: http
  seq: 1
}

get {
  url: {{url}}?address=Αργους 77 Αθηνα
  body: none
  auth: inherit
}

params:query {
  address: Αργους 77 Αθηνα
}

script:post-response {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
}
