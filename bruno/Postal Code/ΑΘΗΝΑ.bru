meta {
  name: ΑΘΗΝΑ
  type: http
  seq: 2
}

get {
  url: {{url}}?address=Αργους 77 Αθήνα
  body: none
  auth: inherit
}

params:query {
  address: Αργους 77 Αθήνα
}

script:post-response {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
}
